import styled from 'styled-components';

const Button = styled.button.attrs({
  type: 'button',
})`
  height: 40px;
  padding: 0 10px;
  display: flex;
  align-items: center;
  justify-content: center;
`;
